{
    "assert.active": "Enable Zend assertions (On/Off). Disabled in production for performance.",
    "assert.bail": "Stop execution on failed assertion (On/Off).",
    "assert.callback": "User callback invoked on assertion failure. Empty to disable.",
    "assert.exception": "Throw AssertionError on failed assertion instead of warning (On/Off).",
    "assert.quiet_eval": "Suppress errors from eval() used by assert(). Deprecated/removed.",
    "assert.warning": "Emit a warning on failed assertion (On/Off).",
    "zend.assertions": "Controls compilation of assert(): 1=generate, 0=optimize, -1=remove.",
    "cgi.check_shebang_line": "Respect shebang (#!) line in scripts when running CGI (0/1).",
    "cgi.discard_path": "Ignore script filename in PATH_INFO for security (0/1).",
    "cgi.fix_pathinfo": "Enable PATH_INFO processing under CGI (0/1).",
    "cgi.force_redirect": "Force redirect to prevent unauthorized CGI invocation (0/1).",
    "cgi.nph": "Use non-parsed headers mode for CGI (0/1).",
    "cgi.redirect_status_env": "Environment variable name to pass redirect status; empty disables.",
    "cgi.rfc2616_headers": "Send Status header in RFC 2616 format (0/1).",
    "doc_root": "Document root directory for PHP scripts; empty to accept server default.",
    "extension_dir": "Directory where PHP looks for loadable extensions (.so/.dll).",
    "include_path": "List of directories for require/include, separated by PATH_SEPARATOR.",
    "sys_temp_dir": "System temporary directory used by PHP.",
    "upload_tmp_dir": "Temporary directory for HTTP file uploads; falls back to system temp if empty.",
    "user_dir": "User directory name for ~username style paths (Apache only).",
    "default_charset": "Default output charset for HTML and other content (e.g., UTF-8).",
    "default_mimetype": "Default MIME type for output (e.g., text/html).",
    "input_encoding": "Default input encoding for multibyte support; empty auto-detect or default.",
    "internal_encoding": "Default internal string encoding for mbstring; empty to use default.",
    "output_encoding": "Default output encoding for mbstring; empty to use default.",
    "zend.detect_unicode": "Detect Unicode BOM at script start (On/Off).",
    "zend.multibyte": "Enable multibyte script encoding detection (On/Off).",
    "zend.script_encoding": "Script encoding name when zend.multibyte is enabled (e.g., SJIS, EUC-JP).",
    "display_errors": "Display runtime errors to output (On/Off). Off in production.",
    "display_startup_errors": "Display PHP startup sequence errors (On/Off).",
    "docref_ext": "Append extension to docref links (e.g., .html) in error messages.",
    "docref_root": "Base URL for documentation references in error messages.",
    "error_append_string": "String appended to error messages output.",
    "error_log": "File path or syslog for error logging; leave empty to use SAPI logging.",
    "error_prepend_string": "String prepended to error messages output.",
    "error_reporting": "Error levels to report (bitmask). Common: E_ALL, exclude with ~E_NOTICE, ~E_DEPRECATED.",
    "html_errors": "Format errors as HTML (On/Off).",
    "ignore_repeated_errors": "Suppress repeated identical error messages (On/Off).",
    "ignore_repeated_source": "Ignore source line in repeat error detection (On/Off).",
    "log_errors": "Log errors to error_log (On/Off).",
    "log_errors_max_len": "Max length of each logged error line in bytes (0 for unlimited).",
    "report_memleaks": "Report memory leaks detected by Zend MM (On/Off).",
    "report_zend_debug": "Include Zend Engine debug info in error messages (On/Off).",
    "track_errors": "Store last error message in $php_errormsg (On/Off). Removed in PHP 8.",
    "windows.show_crt_warning": "Suppress MSVC CRT warnings on Windows (On/Off).",
    "xmlrpc_error_number": "Custom XML-RPC fault code integer.",
    "xmlrpc_errors": "Send XML-RPC errors as XML faults instead of standard errors (On/Off).",
    "zend.exception_ignore_args": "Hide function arguments in exception traces for privacy (On/Off).",
    "zend.exception_string_param_max_len": "Max string length for arguments in exception messages/traces.",
    "allow_url_fopen": "Allow URL-aware fopen wrappers (http, ftp, etc.) (On/Off).",
    "allow_url_include": "Allow include/require via URL wrappers (On/Off). Security risk when On.",
    "auto_detect_line_endings": "Detect Macintosh/Unix/DOS line endings in text files (On/Off).",
    "auto_globals_jit": "Delay initialization of $GLOBALS like $_SERVER until used (On/Off).",
    "engine": "Enable PHP engine (1) or disable to pass-through files (0).",
    "enable_dl": "Allow dl() to load extensions at runtime (On/Off). Disabled on thread-safe/most SAPIs.",
    "enable_post_data_reading": "Automatically read POST data for php://input and $_POST (On/Off).",
    "expose_php": "Add X-Powered-By header with PHP version (On/Off).",
    "file_uploads": "Enable HTTP file uploads (On/Off).",
    "ignore_user_abort": "Continue script execution even if client disconnects (On/Off).",
    "implicit_flush": "Flush output after every output call (On/Off).",
    "last_modified": "Set Last-Modified header automatically (timestamp or 0 to disable).",
    "register_argc_argv": "Populate $argc and $argv in non-CLI SAPIs (On/Off).",
    "short_open_tag": "Allow short open tag '<?' for PHP code (On/Off). Always allowed for '<?='.",
    "default_socket_timeout": "Default timeout (seconds) for socket-based streams.",
    "hard_timeout": "SAPI-specific hard timeout for request handling (seconds).",
    "max_execution_time": "Maximum script execution time in seconds (0 for unlimited).",
    "max_file_uploads": "Maximum number of files allowed per upload request.",
    "max_input_nesting_level": "Max nesting level of input variables arrays (e.g., $_GET depth).",
    "max_input_time": "Max time in seconds to parse input data (POST/GET) (-1 unlimited).",
    "max_input_vars": "Max number of input variables accepted (per request).",
    "max_multipart_body_parts": "Max parts allowed in multipart/form-data (-1 unlimited).",
    "memory_limit": "Max memory a script may consume (e.g., 128M, -1 unlimited).",
    "output_buffering": "Enable output buffering (On/Off or size in bytes).",
    "post_max_size": "Max size of POST data allowed (e.g., 8M). Must be >= upload_max_filesize.",
    "precision": "Number of significant digits for floating point output.",
    "realpath_cache_size": "Size of realpath cache (e.g., 4096K).",
    "realpath_cache_ttl": "TTL for realpath cache entries in seconds.",
    "serialize_precision": "Precision for serialize()/json_encode floats (-1 uses full precision policy).",
    "unserialize_max_depth": "Max depth allowed during unserialize() to prevent recursion attacks.",
    "upload_max_filesize": "Max size for uploaded files (e.g., 2M).",
    "user_ini.cache_ttl": "TTL in seconds for user INI (.user.ini) files.",
    "mail.add_x_header": "Add X-PHP-Originating-Script header to mail() (On/Off).",
    "mail.force_extra_parameters": "Extra command-line params for sendmail (e.g., -f sender).",
    "mail.log": "Log each mail() call to file or syslog.",
    "mail.mixed_lf_and_crlf": "Allow mixed LF and CRLF line endings in mail headers (On/Off).",
    "sendmail_from": "Default From address for Windows mail().",
    "sendmail_path": "Path to sendmail-compatible binary and flags (e.g., /usr/sbin/sendmail -t -i).",
    "SMTP": "SMTP server hostname for Windows mail().",
    "smtp_port": "SMTP server port for Windows mail() (default 25).",
    "disable_classes": "Comma-separated list of classes to disable.",
    "disable_functions": "Comma-separated list of functions to disable (e.g., exec, system).",
    "open_basedir": "Restrict file operations to specified directory tree(s); semicolon separated.",
    "syslog.facility": "Syslog facility to use (e.g., LOG_USER, LOG_MAIL, LOG_LOCAL0..7).",
    "syslog.filter": "Syslog filter mode (e.g., no-ctrl to remove control chars).",
    "syslog.ident": "Syslog ident string (tag) used for log entries.",
    "zlib.output_compression": "Compress output using zlib (On/Off or level).",
    "zlib.output_compression_level": "Compression level for zlib output (-1 default, 0..9).",
    "zlib.output_handler": "Output handler for zlib (e.g., ob_gzhandler). Mutually exclusive with output_handler.",
    "arg_separator.input": "Input argument separator(s) for URL parsing (default '&').",
    "arg_separator.output": "Separator used when generating URLs (default '&').",
    "auto_append_file": "Script appended automatically to output at request end.",
    "auto_prepend_file": "Script prepended automatically before main script.",
    "browscap": "Path to browscap.ini for user agent capability detection.",
    "cli_server.color": "Colorize CLI built-in webserver output (On/Off).",
    "fiber.stack_size": "Default stack size for Fibers in bytes (empty = engine default).",
    "from": "Define the anonymous ftp password (your email address)",
    "highlight.comment": "Syntax highlight color for comments (HTML color).",
    "highlight.default": "Syntax highlight color for default text (HTML color).",
    "highlight.html": "Syntax highlight color for HTML (HTML color).",
    "highlight.keyword": "Syntax highlight color for keywords (HTML color).",
    "highlight.string": "Syntax highlight color for strings (HTML color).",
    "output_handler": "Output buffering handler (e.g., ob_gzhandler). Avoid combining with zlib.output_handler.",
    "request_order": "Order of populating $_REQUEST (e.g., GPC, GP).",
    "unserialize_callback_func": "Callback called when unserialize() encounters undefined class.",
    "url_rewriter.hosts": "Hostname whitelist for transparent SID rewriting across hosts.",
    "url_rewriter.tags": "HTML tags/attributes to rewrite with session ID (e.g., form=, a=href).",
    "user_agent": "Default HTTP User-Agent header for stream wrappers.",
    "user_ini.filename": "Filename for per-directory user INI files (e.g., .user.ini).",
    "variables_order": "Order to register EGPCS variables (Environment, GET, POST, Cookie, Server).",
    "zend.enable_gc": "Enable Zend Garbage Collector for cyclic garbage (On/Off).",
    "zend.max_allowed_stack_size": "Maximum allowed C stack size per request in bytes (-1 unlimited).",
    "zend.reserved_stack_size": "Reserved C stack space to safely handle stack overflows (bytes)."
}