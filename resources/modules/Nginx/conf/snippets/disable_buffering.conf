# Disable buffering only for streaming or long-lived endpoints.
# Do not include globally.

fastcgi_buffering                off;
proxy_buffering                  off;
proxy_request_buffering          off;
