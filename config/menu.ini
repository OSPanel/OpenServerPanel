[domains\{lang_199}]

command              = http://{host}
need_active_state    = on

[domains\{lang_200}]

command              = "%COMSPEC%" /k "osp project {host_menu_name}"

[domains\{lang_201}]

work_directory       = {host_root_dir}

[modules\{lang_216}]

command              = "%COMSPEC%" /k "osp set {module_name}"

[modules\{lang_217}]

command              = "%COMSPEC%" /k "osp shell {module_name}"
need_module_shell    = on

[modules\{lang_218}]

work_directory       = {root_dir}\data\{module_name}

[modules\{lang_219}]

work_directory       = {root_dir}\config\{module_name}\{module_profile_name}

[modules\{lang_220}]

hr_after             = on
work_directory       = {root_dir}\modules\{module_name}

[modules\{lang_221}]

command              = "%COMSPEC%" /k "osp init {module_name}"
need_inactive_state  = on

[modules\{lang_222}]

command              = "%COMSPEC%" /k "osp on {module_name}"
need_active_type     = on
need_inactive_state  = on

[modules\{lang_223}]

command              = "%COMSPEC%" /k "osp restart {module_name}"
need_active_state    = on
need_active_type     = on

[modules\{lang_224}]

command              = "%COMSPEC%" /k "osp off {module_name}"
need_active_state    = on
need_active_type     = on

[{lang_161}]

hr_before            = on
command              = {web_panel_url}

[{lang_162}]

command              = "%COMSPEC%" /k "osp reset init"

[{lang_163}]

work_directory       = {root_dir}

[{lang_168}]

command              = syspreptool.exe
hr_after             = on
hr_before            = on