[full-example.local]

Launch Node app = * * * * * 1 0 "%COMSPEC%" /c "osp project {host} start"
Backup = */5 * * * * * 0 "%COMSPEC%" /c "tar -czf {base_dir}\.osp\backup\{host_decoded}.tar.gz -C {public_dir} *"

[forum.full-example.local]

Backup = */5 * * * * * 0 "%COMSPEC%" /c "tar -czf {base_dir}\.osp\backup\{host_decoded}.tar.gz -C {public_dir} *"
